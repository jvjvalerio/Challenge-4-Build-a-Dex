import { useDebugValue } from 'react';
import { map } from 'rxjs/operators';
import { useObservableState } from './use-observable-state';
import { useObservable } from './use-observable';
export function useObservableGetState(state$, initialState, ...path) {
    const value = useObservableState(useObservable(() => state$.pipe(map(state => path.reduce(getValue, state)))), initialState);
    useDebugValue(value);
    return value;
}
function getValue(obj, key) {
    return obj[key];
}
//# sourceMappingURL=use-observable-get-state.js.map